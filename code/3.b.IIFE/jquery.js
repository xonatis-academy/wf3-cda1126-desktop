// il faut donner un i pour multiplier par 2

var double = i * 2;

function faireSomme(tab) {
    var somme;
    while (i < tab.length) {
        somme = somme + tab[i];
        ++i;
    }
    return somme;
}

